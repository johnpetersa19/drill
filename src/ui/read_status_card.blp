using Gtk 4.0;
using Adw 1;

// ReadStatusCard
// Shows a small dot + status text (no progress bar, no percentage)
// States: waiting (gray) | reading (pulsing) | done (green) | error (red)
// TODO: implement
